---
title: Debian 软件仓库镜像使用帮助
short_title: Debian 软件仓库
excerpt: 西安交通大学软件镜像站 Debian 软件仓库镜像使用帮助。
alias:
  - debian
---

## 基本信息

Debian 是一个自由的操作系统，由 Debian 计划开发和维护。Debian 是一个自由的 Linux 发行版，添加了数以千计的应用程序以满足用户的需要。

### 收录架构

- i386
- x86_64
- source

### 收录版本

- stable
- sid
- testing
- experimental

## 使用说明

如果遇到无法拉取 https 源的情况，请先使用 http 源并安装：

```bash
sudo apt install apt-transport-https ca-certificates
```

[//]: # "{ id: 'bionic', title: '18.04 LTS (Bionic Beaver)' },"
[//]: # "{ id: 'xenial', title: '16.04 LTS (Xenial Xerus)' },"
[//]: # "{ id: 'trusty', title: '14.04 LTS (Trusty Tahr)' },"

### 配置生成器

export const data = [
  { id: 'stable', title: 'Stable' },
  { id: 'sid', title: 'Sid' },
  { id: 'testing', title: 'Testing' },
  { id: 'experimental', title: 'experimental' },
];

export const pattern = (
  id,
  enableSource,
) => `# Config generated by XJTU Mirrors Config Generator; DO NOT EDIT.\n
deb https://mirrors.xjtu.edu.cn/debian ${id} main contrib non-free
${
  enableSource ? '' : '# '
}deb-src https://mirrors.xjtu.edu.cn/debian ${id} main contrib non-free
deb https://mirrors.xjtu.edu.cn/debian ${id}-proposed-updates main contrib non-free
${
  enableSource ? '' : '# '
}deb-src https://mirrors.xjtu.edu.cn/debian ${id}-proposed-updates main contrib non-free
deb https://mirrors.xjtu.edu.cn/debian ${id}-updates main contrib non-free
${
  enableSource ? '' : '# '
}deb-src https://mirrors.xjtu.edu.cn/debian ${id}-updates main contrib non-free
`;

export const ConfigGenerator = () => {
  const [state, setState] = React.useState({
    version: 0,
    enableSource: false,
  });
  return (
    <div>
      <p>
        选择 Debian 版本：
        <select
          value={state.version}
          onChange={(e) => setState({ ...state, version: e.target.value })}
        >
          {data.map((item, key) => (
            <option key={key} value={key}>
              Debian {item.title}
            </option>
          ))}
        </select>
        <br />
        <span className="space-x-2">
          <label className="text-sm">
            <input
              name="enableSource"
              type="checkbox"
              checked={state.enableSource}
              onChange={(e) =>
                setState({ ...state, enableSource: e.target.checked })
              }
            />
            使用软件源码镜像
          </label>
        </span>
      </p>
      <pre>
        <code className="language-shell">
          {pattern(data[state.version].id, state.enableSource)}
        </code>
      </pre>
    </div>
  );
};

<ConfigGenerator />

再运行`apt-get update`生成缓存。
